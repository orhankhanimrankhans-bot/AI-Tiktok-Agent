"""Graphics scene type reserved for workflow graph items and scene events."""
from PySide6.QtWidgets import QGraphicsScene
class WorkflowScene(QGraphicsScene):
    """Owns workflow graphics items independently from persisted models."""
    pass
